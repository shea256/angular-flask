'use strict';

/*
angular.module('angServices', ['ngResource']).
	factory('Thing', function($resource) {
		return $resource('api/Thing/:id', {}, {
			query: {
				method: 'GET',
				params: { id: '' },
				isArray: true
			}
		})
		;
	});
*/